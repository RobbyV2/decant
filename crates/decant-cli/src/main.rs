//! # decant-cli (Phase 1)
//!
//! The user's hands-on tool for driving the daemon and verifying the live VM.
//! Subcommands (Phase 1+): `processes`, `modules`, `read`, `write`, `scan`,
//! `resolve`, `diagnostics`. Phase 0 ships a stub so the workspace builds.

fn main() {
    eprintln!("decant-cli: not implemented yet (Phase 1).");
    std::process::exit(0);
}
